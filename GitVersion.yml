next-version: 1.0.0
mode: ContinuousDeployment
assembly-versioning-scheme: MajorMinorPatch
assembly-file-versioning-scheme: MajorMinorPatch
branches:
  main:
    regex: ^main$
    increment: Patch
    prevent-increment-of-merged-branch-version: true
  develop:
    regex: ^develop$
    increment: Minor
    tag: beta
  feature:
    regex: ^feature[/-]
    increment: Inherit
    source-branches: ['develop']
  release:
    regex: ^release[/-]
    increment: None
    source-branches: ['develop', 'main']
  hotfix:
    regex: ^hotfix[/-]
    increment: Patch
    source-branches: ['main']

ignore:
  sha: []

commit-message-incrementing: Disabled

# Custom date format for the versioning scheme.
commit-date-format: yyyy.MM.dd.HHmm

# Customize the version format for informational purposes.
assembly-informational-format: '{Major}.{Minor}.{Patch}{BranchName}-{CommitsSinceVersionSource}-{CommitDate}'
